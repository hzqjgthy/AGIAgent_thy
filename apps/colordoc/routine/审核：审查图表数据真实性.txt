阅读用户提出的图表，如果图为svg，则直接读，如果为代码生成的图则阅读对应代码。
阅读manager.out文件，理解文档的产生过程，用文字总结可能的数据来源（如用户上传的文件数据或网络数据）
总结图表中涉及到的真实数据信息，列写到data.csv文档的第一列（需照抄原文以便于后期寻找），并在第二列中标记未验证。
阅读web_search_result中的txt文件，这些文件代表了获取到的互联网数据信息。
如果用户提及了上传的文件源或manager.out提到了读取的用户文件，则需要读取该文件源，看看是否存在相关数据。
修改data.csv，将其中已被核实存在的数据，在第二列中标记为已验证。
对于未核实到的数据，进行互联网搜索或数据源验证，查找是否存在相关的数据，如果确认原数据正确，则在第二列中标记为已验证，
如果找到了相关数据，但原数据有误差，则在第二列中标记需修改，并在第三列中填写修改后的描述。
遍历csv中的所有数据，直到都被验证过为止。
将csv中标记为需修改的数据，用修改后的描述替换原图表的产生源文件，例如svg文件，代码文件。
将csv中标记为未验证的数据，在原稿中进行模糊化描述处理。
如果用户没有提及任何图表，则默认处理该工作目录下的所有svg/png图表，并提示用户可以从工作目录中拖拽一个文档到需求框用于指明文档名。


阅读用户提出的文档。总结涉及到的真实引用链接，列写到data.csv文档的第一列（需照抄原文以便于后期寻找），并在第二列中标记未验证。
阅读manager.out文件，理解文档的产生过程，用文字总结可能的数据来源（如用户上传的文件数据或网络数据）。
阅读web_search_result中的txt文件，这些文件代表了获取到的互联网数据信息。
如果用户提及了上传的文件源或manager.out提到了读取的用户文件，则需要读取文件源，看看是否存在相关数据修改data.csv，将其中已被核实存在的数据，在第二列中标记为已验证。
对于未核实到的数据，进行互联网搜索，查找是否存在相关的数据，如果确认原数据正确，则在第二列中标记为已验证，
如果找到了相关数据，但原数据有误差，则在第二列中标记需修改，并在第三列中填写修改后的描述。
遍历csv中的所有数据，直到都被验证过为止。
将csv中标记为需修改的数据，用修改后的描述替换原文。
将csv中标记为未验证的数据，在原稿中进行模糊化描述处理。
如果用户没有提及任何文档，则停止工作并提示用户需要明确阅读哪个文档，并提示用户可以从工作目录中拖拽一个文档到需求框用于指明文档名。

